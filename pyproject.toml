[project]
name = "conflict-map"
version = "0.1.0"
description = "Local news-driven world map of current conflicts"
requires-python = ">=3.11"
dependencies = [
    "fastapi>=0.115",
    "uvicorn[standard]>=0.30",
    "httpx>=0.27",
    "feedparser>=6.0",
]

[tool.uv]
package = false
